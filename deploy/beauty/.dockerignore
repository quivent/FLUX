# Build context for deploy/beauty/Dockerfile. Keep it to the Tea surface, the
# web assets, and the staged binaries — the repo's model outputs and caches must
# never enter a layer.
*
!apps/tea/**
!web/**
!deploy/beauty/**
outputs/
.venv/
.fluxd/
**/.git/
**/node_modules/
**/__pycache__/
